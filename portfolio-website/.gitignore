# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.next
out
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Agent tooling: installed skills and design references (third-party, not part of the site)
.agents/
# Symlinks into .agents/skills/ above — dangling on any clone that doesn't have it
.claude/skills/

# Isolated build output and browser-automation scratch (agent tooling)
.next-verify/
.playwright-cli/
